type Mutation {
  createUser(input: UserInputType!): UserType
}

type Query {
  users: [UserType]
}

"""User payload definition"""
input UserInputType {
  email: String!
}

"""User信息"""
type UserType {
  id: ID!
  name: String
  password: String
}
